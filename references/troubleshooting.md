# 排障

只有发生运行错误、性能异常或检查结果矛盾时读取本文件。

## 先判断问题属于哪一层

| 现象 | 优先检查 |
|---|---|
| 人物/体型/构图一开始就错 | 关键帧提示词、参考图职责、资源本身 |
| 首帧正确，视频后段人物漂移 | H3 模式、身份参考、动作复杂度 |
| 台词没说完 | 导演时长、生成预算、裁切点 |
| 动作飞掉或主体融合 | 单元内高风险转换过多 |
| 视频生成特别慢 | profile、尺寸、步数、工作流是否走错 |
| 画面烧字幕 | H3 固有行为，走后期过滤 |
| 有文件但播放异常 | 完整解码、音视频编码参数 |
| 报连接错误 | ComfyUI 是否真正 ready，而不只是端口打开 |

## ComfyUI

- 端口能连接不等于模型和节点已经加载完成；使用 readiness 检查。
- 同一时间只允许一个视频生成任务。
- 工作流参数必须同时传到外层工具和 ComfyUI 节点，避免命令显示一个尺寸、节点实际使用另一个尺寸。
- 性能突然从约百秒变成数百秒时，先核对是否误用基础 H3、错误 profile、错误分辨率或稀疏注意力没有启用。

## 进程与日志

- PowerShell 管道使用 `Select-Object -First` 可能提前终止上游进程，不能用它判断长任务真实退出原因。
- 不要只凭一个目录快照判断是否生成成功；覆盖同名文件时应读取工具返回的实际产物路径和修改时间。
- 停止任务前确认具体进程，避免影响用户已经启动的其他 ComfyUI 工作。

## 画面与尺寸

- 使用 `cli/inspect.mjs` 查看真实分辨率和抽帧，不现场拼临时 ffmpeg 命令。
- 不根据一次显存扫描宣布某尺寸“稳定可用”；以官方规格和真实工作流验证为准。
- 缩略图只能定位问题，涉及五官、服装或文字时打开原图。

## 音频

- 响度数字正常不代表人耳听起来正常，最终必须实听。
- 拼接前统一采样率、声道和编码；不要对不兼容的 AAC 片段直接流复制。
- H3 原声与 TTS 同时存在会产生双重人声。先明确采用原声、替换还是混音。
- 使用混音时注意默认归一化可能降低音量，并在末端做响度归一化。

## 字幕过滤

- 保存原片，再生成去字幕版本。
- VSR 或修复插件可能输出非交付编码，合成前重新编码为目标格式。
- 字幕过滤改变了视频文件，因此对应人工确认票应重新建立。

## 升级为规则的标准

某个故障只有满足以下条件才进入当前 reference：

1. 可复现；
2. 会改变后续决策；
3. 不能仅靠代码和测试彻底消除。

单次项目的具体耗时、文件名和视觉评价留在该剧目的 `reviews/` 中，不进入源码仓库，也不要继续膨胀主 Skill。
