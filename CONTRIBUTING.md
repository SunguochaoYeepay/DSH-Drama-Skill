# 贡献与改动规程

这份文件写给两类人：要给仓库提改动的协作者，以及正在干活的 Agent。**规矩不多，但每条都是吃过亏换来的。**

---

## 一、先分清「改代码」和「写经验」

改动按风险分成两层，边界是硬的：

| 层 | 范围 | 谁说了算 |
|---|---|---|
| **沉淀层**（随便写） | 剧目的 `reviews/`、`.workbuddy/memory/<日期>.md`、CHANGELOG | 不需要任何人同意，写完即沉淀 |
| **禁区**（一个字都不许动） | ① 工程代码 `src/ cli/ tests/ schema/ skills/ plugins/`<br>② 规则正本 `references/` 下全部 `.md`<br>③ 逐字送模型的文件（`projects/<剧>/keyframe-prompts/*.txt` 等） | 必须先提**提案**，拿到答复才动 |

为什么连 `references/` 都算禁区：**改规则 = 间接改下一部剧的提示词**。规则改一个词，三个月后写的提示词就跟着变，
源头却早没人记得。所以要走同一道闸。

### 提案要写四要素

缺一项等于没提：

1. **目标文件的小节** —— 精确到哪一节，不接受「某个文档里」；
2. **改前原文** —— 逐字贴出来；
3. **改后文字** —— 逐字写出替换后的样子；
4. **依据** —— 哪个事件、是否复发。（只发生一次的，通常不值得写进规则。）

> 未答复 = 不改。**被否决 = 不改，且同一轮里不再提交同一条。**

由此推出两条：提示词文件**永不回溯改写**（要变就重跑那个单元）；`references/` 的改动同样要过闸。

---

## 二、人工票谁都不许代签

剧本、板子、导演稿、资源、关键帧、每段视频、成片，每个阶段都要人个点头。

- `review.approvals.json` **只由 `cli/review-gate.mjs` 在用户明确说「通过」之后落笔**。
- Agent、脚本、示例数据都**不能替用户写票**。示例剧目宁可停在没票的地方，也不伪造一张。
- 产物被替换后旧票自动失效（哈希绑定），所以"手改一下产物"不能绕过确认。

**工程没有机器审核**：没有视觉模型打分、没有成片自检。闸门唯一的作用是防止「确认了 A、交付了 B」，
它不判断质量——好不好看由人说了算。请不要往里加自动评分。

---

## 三、测试：判据是「它守护的行为今天还在不在」

全绿不等于有保护。按下表分档处置：

| 情况 | 处置 |
|---|---|
| 纯函数、CLI 实际输出、产物行为 | 保留（这是真测试） |
| 反向断言（"不应再因机器检查拒绝"） | 保留 |
| **源码文本断言**（正则查源码里有没有这行字） | 不测行为，**不再新增**；已有的能行为化就改写，不能就删 |
| 函数已删，只剩测试守着 | 测试与函数一起删，不留孤儿测试 |
| 零引用的模块 | ≠ 僵尸。可独立运行的入口、未接线的能力、模块公共 API、契约常量都属于正常存在 |

几条硬规矩：

- **新增能力必须带测试**，不然不许合。
- **CLI 怎么测**：脚本式 `cli/*.mjs` 内部函数 import 不出来。通用做法是让 CLI 在 `--dry-run` 下
  打印它真正会用的参数，再断言那行输出——**不要断言源码文本**。
- 测试**自带输入**：一律用 `os.tmpdir()` 造临时目录或 `tests/fixtures/`，**不许指向本机真实工作区**
  （那会随工作区迁移再次失效）。
- 碰回收站 / 到期的用例一律走**注入的假回收站**（`startServer({ trash })`），不许污染真回收站。
- 跑全量：`npm test`（递归跑 `tests/` 下所有 `*.test.mjs`，新增文件无需维护清单）。

---

## 四、提交下来的东西要过一遍

- **剧目绝对不许进仓库**：`projects/**` 整体忽略，只有说明用的 README 例外。
  这是为了让 clone 仓库的人拿到的是**能跑的流水线**，不是我的剧。
- **媒体不入库**：图 / 视频 / 音频连传记都不许进。
- **凭据不入库**：`.env`、`*.key`、`*.pem`。
- 往 `.gitignore` 加目录规则时，**必须带 `/` 前缀锚到仓库根**。裸名是「任意层级同名目录」的意思，
  历史上这里写过 9 条裸名，`web/src/out/`、`tests/fixtures/assets/` 这类正常源码目录被静默吞掉。
  另外 **gitignore 不支持行尾注释**，`#` 必须独占一行。

---

## 五、日常操作纪律

1. 阶段产物出来后**先把实际产物摊给人看**（图片给原图、视频给完整视频），再谈下一步。只给路径不算送审。
2. 一段视频确认后才生成下一段；**批票与启动下游不能是同一个动作**。
3. 说不清的长思考，**中间要有阶段性进度消息**，不要让人对着空转的窗口等。
4. 改默认档位要**同时报收益和风险**，不能只讲一边。
5. 用户回字母 / 序号时，先回显「字母 → 具体产物文件（路径 + sha）」再动手。
6. 涉及**人设、剧情、画幅、风格、花钱**才停下来问；0 成本且不改语义的直接做，做完报告。
