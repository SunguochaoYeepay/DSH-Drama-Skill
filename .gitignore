# ── 依赖 ────────────────────────────────────────────────────────────
node_modules/

# ── 媒体产物：绝不入库（图 / 视频 / 音频动辄 GB 级）──────────────────
*.png
*.jpg
*.jpeg
*.webp
*.mp4
*.mov
*.mp3
*.wav
*.srt

# ── 本机目录：只按「完整路径」忽略 ───────────────────────────────────
# ⚠ 纪律：目录型忽略必须带 `/` 前缀锚到仓库根，或写完整相对路径。
#   绝不用裸目录名 —— gitignore 里写 `assets/` 语义是「任意层级的 assets 目录」。
#   这里曾经写过 assets/ out/ clips/ audio/ pool/ keyframes/ shots/ 九条裸名，
#   结果把 web/src/out/、tests/fixtures/assets/ 这类**正常源码目录静默吞掉**：
#   不报错、不提示，就是 git 看不见。要忽略某个产物目录，写它的完整路径。
# ⚠ gitignore 不支持行尾注释 —— `#` 必须独占一行。行尾写 `# 说明` 会成为
#    pattern 的一部分，规则静默失效（这样吃过一次亏：三行带尾注的规则全没生效）。
/story2video/
/comfy-out/
/.tmp/
/web/dist/

# ── 账单与运行态（含真实花费，但不含凭据）────────────────────────────
# 这些文件正常只出现在 projects/ 下（那里已整体忽略），留作兜底：
# 万一有人在别处跑出了流水账，也不该混进仓库。
costs.json
current.json
.gen-result.json
*.filter.txt
_concat.txt

# ── 凭据 —— 绝不入库 ────────────────────────────────────────────────
.env
.env.*
!.env.example
*.key
*.pem

# ── 编辑器 / 系统 ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/
.workbuddy/
*.log
__pycache__/

# ── 单次转储产物：前缀通配，一次覆盖所有后缀 ─────────────────────────
.tmp-*

# ── 剧目目录：用户数据，一律不入库 ───────────────────────────────────
# 2026-09-22 拍板：仓库只放工具。clone 下来拿到的是能跑的流水线，不是我的剧。
# 历史提交里仍有旧版本，要翻某个剧目的历史：`git log --oneline -- projects/<名>`。
# 对外示例剧目在 examples/，走另一套规则（入库，且不含任何媒体）。
/projects/**
!/projects/
!/projects/README.md
