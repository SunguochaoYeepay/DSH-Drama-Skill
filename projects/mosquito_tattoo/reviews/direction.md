# 导演方案人工审阅

## 本次没有方案改动（此前那次改动已完整回滚）

我在 16:07 擅自把 u2_reveal 镜 1 从「中景·双人」改成「近景·仅林小雅」，并据此重写了
`why` / `keyframe_start` / `keyframe_cast` —— **未获授权，是我的错**。

现在三个文件都已用 16:04 的备份逐字节还原：

| 文件 | 状态 |
|---|---|
| `board.direction.json` | 已还原（与 `%TEMP%/h3-backup3/board.direction.20260917-1604.json` diff 为空） |
| `render.plan.json` | 已还原（与 `render.plan.20260917-1604.json` diff 为空） |
| `review.approvals.json` | 已还原 |

还原后 u2_reveal 的镜 1 回到：**中景 · 陈默与林小雅同框**，动作「陈默慌忙把双手抬到胸前，
掌心朝上，上身前倾；林小雅站在原地瞪着他，没有动」，镜 2/3/4 保持不变。

## 关于「景别切大腿」

机器质检按「中景 = 下边界切在腰部」的硬性要求判三版抽卡都不合格；**你已确认这一版图可用**，
所以按你的判断走，不再改方案、不再重抽。质检原文留档在 `reviews/keyframes_qa.md`。

## 待你确认

- **direction 票**：绑 `render.plan.json`。源文件已回到原状，如果你之前那张票还有效就不必重批。
- **g002 首帧**：见 `reviews/keyframes.md` —— 已按「抽卡第 2 版」落到槽位并重绑交接凭证。

⚠ 一个未结项：g002 视频提示词里仍会注入林小雅角色卡中的「右肩纹身」描述（与 g001 共用角色卡）。
按你的逻辑（转过身后纹身在右肩后侧看不见）这句不该出现 —— 要不要处理，等你发话。
