# projects/ — 剧目目录（只在本地，不进 git）

每个子目录是一部剧：剧本、板子、导演稿、生成计划、提示词、人工票、图与视频产物全在这里。

**这个目录不进 git**（2026-09-22 拍板：仓库只放工具，剧的产物只在本地）。
- 剧目搬进/搬出：整目录拷贝即可，看板按 `board.json` 识别剧目；
- 归档的剧目在 `projects/_archive/`，满 7 天自动进系统回收站；
- 立项用 `node cli/init-board.mjs`，它会写下 `meta.created_at`（列表排序的正主）。
