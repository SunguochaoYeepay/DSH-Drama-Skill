# ai-images-harness — 故事到片子，agent 原生

> ## ⚠ 一条被推翻的结论（必读）
>
> 工程里曾写着「**H3 会把对白烧成字幕、压不住，只能裁掉底部 13%**」，
> 而且据此给每一镜都裁了一刀。**这个结论复现不出来。**
>
> 复盘时把工作区所有旧片段扫了一遍：
>
> | 检查对象 | 结果 |
> |---|---|
> | 8 个旧片段，各抽 1 帧看中部 45% | **无字幕** |
> | 最早那批，各看底部 28%（放大） | **无字幕** |
> | s05 最长镜（12.7s），全时段 12 帧 | **无字幕** |
>
> **而真正的违规在提示词格式上**（见下）。按官方格式修好之后，字幕更没有出现的理由 ——
> 官方规范是**显式声明制**：屏上文字必须用引号写出来才会出现。
>
> **一个没被证实的 13% 裁切，比一个假想的字幕更糟。** 现在默认**不裁**，
> 真要裁显式加 `--crop-caption`。
>
> **教训**：写下一条"压不住、只能绕过"的结论之前，先把它复现一遍并留下证据。
> 我当初既没留图，也没记录是哪一镜第几秒，结果这条结论在没有证据的情况下活了很久，
> 还带出了一个每镜都执行的副作用。

> **先读这一条，它会省你很多时间。**
>
> ## 别在缩图或聚合数字上下结论
>
> 这个工程里我**同一个错误犯了三次**，每次都是**拿降级的观测当真**：
>
> | # | 我看到的 | 我下的结论 | 真相 |
> |---|---|---|---|
> | 1 | `volumedetect` 报 −20 dB | "音轨正常" | AAC 比特流是坏的，**用户用耳朵发现没声音** |
> | 2 | 760×428 缩图里"白+灰" | "模型给她穿了现代便装" | 原图是**交领+比甲的汉服元素** |
> | 3 | 480px 缩图里"深青色袍子" | "服装描述没照做" | 原图是**月白袍 + 深青腰带**，完全照做了 |
>
> **规矩**：
> - **判断画面 → 开原图。审阅图只用来"找哪里要看"，不用来"下结论"。**
> - **判断音频/编码 → 解到采样或整片解码，别信汇总电平。**
> - 一次观测对不上直觉时，先怀疑观测方式，再怀疑对象。

> **这个工程和 DramaClaw 的区别，一句话：DramaClaw 是给人用的产品，这里是给 agent 用的判断力。**
> DramaClaw 的判断散在几十个 Python 文件的提示词和校验器里，只有人点 UI 才生效；
> 这里把那些判断抽成**规则**，让 agent 读一遍就能自己跑完，人只在闸门上看一眼。

---

## 目录

```
schema/storyboard.schema.json   契约（唯一事实源）
prompts/                        所有 LLM 提示词
  idea2story.md                 点子 → 故事（节拍带 purpose）
  story2board.md                故事 → 分镜（身份/造型/标记/禁含糊）
src/board.mjs                   编译 + 闸门 + 校验 + 补强（CLI 入口）
src/parse-scenes.mjs            场次确定性解析（正则优先，不交给模型）
src/parse-script.mjs            剧本逐行切分（行型判定；台词原文从这儿出来）
src/literal.mjs                 逐行模式：代码切行 → 分组 → **代码搬台词**
src/assets.mjs                  资产配方（**纯函数**：肖像/身份图/场景/道具/关键帧）
src/providers/                  生图通道
  bailian.mjs                   线上：bl image（吃本地路径、多图合成、--n 候选池）
  comfyui.mjs                   本地：comfy-studio gen.py
  index.mjs                     路由（资产走线上 / 关键帧走本地）
src/score.mjs                   候选池逐维打分（本地视觉模型，免费）
src/render.mjs                  资产 / 关键帧 / 出片 / 拼接  ← 候选池已接资产阶段
src/migrate.mjs                 旧契约迁移
tests/                          可编程验收（148 项）
docs/                           从 DramaClaw 提炼的记录
```

**数据不在工程里。** 数据放会话工作区的 `story2video/`（`stories/` `examples/` `shots/`）——
因为 DSH 面板插件是通过 `workspaceFiles` 读的，那是工作区作用域。
**工程 = 代码（位置固定）；数据 = 产物（跟项目走）。**

---

## 四阶段与闸门

```
① 故事 → ② 分镜表 → ③ 资产 → ④ 关键帧 → ⑤ 出片
```

未确认的阶段，下游一律不许消费。`approve` 是那道门，而且**程序不许替你点头** ——
判定标准是「确认之后必须变干净」，不是「确认前必须干净」（后者会死锁：缺票只能靠投票来修）。

### 闸门必须在**花钱的那条路径上**也生效

**这条是补的，而且是我的失误。**

`board.mjs` 里一直有闸门检查，但 **`render.mjs` 从头到尾一次都没查过** ——
I 一口气把资产、关键帧、片段、成片全跑完，中间**没在任何地方停下来让人看一眼**。
四道闸门在渲染路径上全是摆设。

后果就是用户的原话：**"不然你生成了狗屎，后面的视频就更垃圾了。"**
资产不对，后面 14 个关键帧 + 14 个片段（约 11 分钟算力 + 真金白银）**全是白烧**。

现在每个阶段进来第一件事就是查票：

| 阶段 | 要求 | 拦不住会浪费什么 |
|---|---|---|
| `--stage assets` | `story` + `shots` 已确认 | 7 张线上图（约 1.3 元） |
| `--stage keyframes` | 再加 `assets` 已确认 | 14 张关键帧 |
| `--stage clips` | 四道全确认 | 14 个片段（约 11 分钟） |
| `--stage assemble` | 四道全确认 | 前面的全部 |

拦下来时打印**缺哪几票**和**确切的命令**，exit 3。`--skip-gate` 可以绕过，但会大声警告。

### 闸门旁边必须有**能看的东西**

闸门没有审阅物等于没有闸门。每个阶段跑完自动生成一张审阅图：

```
--stage assets     → shots/assets_review.jpg      （肖像/身份图/场景/道具，一行 4 个）
--stage keyframes  → shots/keyframes_review.jpg   （逐个镜头，带景别/时长/台词摘要）
--stage contact    → 只重摆审阅图，不生成任何东西（反复审阅用）
```

图上不写字（ffmpeg 没 fontconfig，`drawtext` 用不了），所以**阅读顺序打到终端**：
左→右、上→下，编号和条目一一对应，最后打印确认命令。

---

## 调用链路复盘（一轮完整梳理的结论）

### 链路全貌

```
剧本.md
  ├─ literal        🟢 纯代码，0 元，<1 秒        32 行 → 14 镜，台词逐字保真
  ├─ assets         🔴 线上 qwen-image-3.0        5 张 · ~0.9 元
  ├─ durations      🟢 纯代码，0 元              本地估每句台词要几秒
  ├─ keyframes      🟢 本地 Qwen-Image-Edit      14 张
  ├─ clips          🟢 本地 H3                   14 个
  └─ assemble       🟢 本地 ffmpeg               裁字幕带 → 混音 → concat → SRT → 自检
```

**线上约 1 元，本地约 20 分钟算力。瓶颈在本地，不在钱。**

### 复盘揪出的四个结构问题

**① TTS 被当秒表用（已修）**

原先：调线上 cosyvoice 合成音频 → ffprobe 量秒数 → 写 `duration_s` → **音频文件丢掉**
（因为默认音轨是 H3 自己生成的人声）。**等于花钱买了一次"量时长"服务。**

改成 `estimateSpeechSeconds()` 本地估算，对 12 条**真实 TTS 实测数据**做二元最小二乘：

```
秒 = −2.237 + 0.2359 × 字数 + 0.9753 × 停顿
停顿 = 句末标点×1 + 逗号顿号×0.6 + 省略号×1.5
```

| 模型 | 平均误差 | 最大 | 偏置 |
|---|---|---|---|
| **本模型** | **0.68s** | 1.56s | **0.00s** |
| 只数字数 | 0.86s | 2.60s | — |
| 旧公式 字数/5.5 | 1.66s | — | — |

整片总长：本地估算 **89.15s** vs TTS 路线 86.67s vs 实际成片 87.94s。

> **踩过的坑**：我一开始怕估短，乘了 1.15 又加 0.5 —— 片子从 87 秒涨到 103 秒，全是空白。
> 而且理由本身站不住：**H3 会把人声塞进我们给的时长**，估短只是"H3 说快一点"，**估长才是真浪费**。
> 呼吸余量只在调用方加一次。

`--stage tts` 仍在，但**只在你想用 TTS 替掉 H3 人声时才跑**，默认不在流水线上。

**② 两个资产是死重（已修）**

`reverse_master` 和 `spatial_layout` 在全工程里只有「定义 / 面板显示 / 日志计数 / 迁移置空」四处引用，
**没有任何一处拿它们生成关键帧** —— `keyframeRefs()` 只读 `sheet` + `scene.master`。
7 张里 2 张白花钱还不参与任何画面。

默认不生成；要接的时候加 `--with-scene-extras`。

**③ 成本/质量的取舍用错了轴（已修）**

```
线上资产   2048～2688px   ← 质量最高，却只花 1.3 元
关键帧     1024×576       ← 本地 4 步 Lightning
成片       864×480        ← 本地 H3 默认
```

**观众看到的是后两层，而我把后两层按"省时间"压到最低配**，同时在线上一层按"省钱"都没敢多用（省 2.5 元）。

**本地算力免费但慢 —— 恰恰应该在那儿换质量。** 现在默认 HQ：

| 档 | 关键帧 | 片段 | 每镜耗时 |
|---|---|---|---|
| 默认（HQ） | 20 步 | **1344×768** | 关键帧 76s / 片段 193s |
| `--fast` | 4 步 | 864×480 | 关键帧 16s / 片段 38s |

H3 实测支持的尺寸：1024×576 / 1152×648 / 960×544 / **1344×768** / 1088×608；
**1280×720 会被 patchify 拒绝。**

**④ 上游一变，下游不失效（未修，结构性问题）**

`durations` 改了 `shot.duration_s` → **已经生成的片段还是旧的**。
目前是**成片自检**在兜（它会报「成片 87.94s 与时间轴 89.15s 差 1.21s」），
但流程本身没有"脏标记"概念。真版本里每个产物该记住自己是用哪一版上游生成的。

### 还有两条待评估

- **打分基本没赚回成本**：只在 `--n > 1` 时触发，区分度差（三个候选都给 9 分），耗时 20-45s/张。
  该做成对比较（DramaClaw 那条），或干脆退到人工闸门。
- **两条编译路线只用了一条**：`from-story`（本地 Ollama）在这条链上闲置，27B 文本模型只用来打分。

---

## 画幅：**先问，别沿用**

我把整条链跑完了才被指出：**短剧是竖屏 9:16，我全程用的是 16:9。**
`meta.aspect` 是从迁移过来的板子上继承的，**我一次都没问过**。

**画幅是所有尺寸的根** —— 它一变，资产、关键帧、片段、成片**全部作废**。
所以这是链路里最该先确认的一个参数。

### ⚠ 两层通道的参数名不一样，**必须都传**

```js
线上 bailian：只认 size   （'1:1' / '16:9' / '9:16'）
本地 comfyui：只认 ratio  （或 width/height）
```

**踩过的坑**：关键帧调用只传了 `ratio` / `width` / `height`（那是给本地的），
线上 `size` 于是走了**默认值 `'16:9'`**：

```
请求里 size = "2688*1536"（16:9，错的）
  → 再被 normalizeSize 裁成 1080×1920
  → increase 放大到 3360×1920，居中裁 1080×1920
  → **只留下 32% 的像素，而且先放大再裁**
```

**13 张关键帧全部中招，每张扔掉 68% 的画面。** 修完后请求里是 `size="1536*2688"`。

**验证方式**（0 成本）：`bailian.dryRun()` 会打出真实请求体 ——
**参数有没有传到，看请求体里那个字段，别信代码"应该会传"。**

写死横屏的地方（都已修成跟着 `meta.aspect` 走）：

| 位置 | 原来的错 |
|---|---|
| `normalizeSize()` | 9:16 的归一化目标只给了 `480:864`（0.41 MP） |
| `buildContactSheet()` | 瓦片固定 16:9，竖图全是黑边 |
| 字幕带裁切 | `scale` 写死 `864×480` |
| `comfyui.edit()` | **根本不传画幅** → 走 `IMAGE_DEFAULT=(1024,576)`，**竖屏会生成成横的** |
| `comfyui.generate()` | `--fast` 写死，成品出不来细节 |
| **关键帧调线上** | **只传 `ratio` 不传 `size`** → 线上走默认 16:9，**扔掉 68% 像素** |

**H3 实测支持的尺寸**（扫描结果，patchify 有约束）：

| 横屏 | 竖屏 |
|---|---|
| 1024×576 ✓ | 480×864 ✓ |
| 1152×648 ✓ | 576×1024 ✓ |
| 1344×768 ✓ | 640×1152 ✓ |
| **1280×720 ✗** | 768×1344 ✓ |
| | **1080×1920 ✓**（2.07 MP，57s） |
| | **720×1280 ✗** |

**`1080×1920` 能跑，而且和 1344×768 一样快** —— 真 1080P 竖屏，这才是短剧该用的。

---

## H3 提示词必须用官方格式

来源：[MiniMax H3 官方 VIDEO_PROMPT_WRITING_GUIDE](https://huggingface.co/MiniMaxAI/MiniMax-H3/blob/main/docs/VIDEO_PROMPT_WRITING_GUIDE_base_en.md)。

**我原先的写法有五处违背规范**，其中前两条合起来就是「字幕」问题的正主：

| # | 官方规范 | 我原来的写法 |
|---|---|---|
| 1 | 台词必须在 `integrated_multimodal_description` 里，包成 `<d>[语言] 台词</d>`，**逐字保留** | 写成散文 `小师妹说：「…」` |
| 2 | **`overall_soundscape` 明令禁止重复台词**（那里只放环境音/动作音/非语言人声） | **台词就塞在 soundscape 里** |
| 3 | 说话者用稳定 ID `(S1)`/`(S2)`，跨镜头不变 | 没有 ID |
| 4 | 画外音必须用固定短语 `says in an off-screen voiceover`，且紧跟一句"嘴唇不动" | 自创「（画外音，嘴唇不动）」 |
| 5 | I2VA 必须带对齐指令 `For the target video, at 0.00 seconds … <Picture 1> …` | 没有 |

还有一条**方向性的**：

**屏上文字是"显式声明制"** —— 官方规定任何 banner / sign / label / subtitle / neon text
都要用**英文双引号写出来**才会出现在画面上。也就是说**不写就不会有**。

所以「写'不要出现字幕'来压字幕」这个思路本身就是错的，而且实测过：
**否定句反而更容易把字画出来**。现在提示词里**一个"字"都不提**。

正确的成品长这样（`h3Prompt()` 产出）：

```
For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced.

integrated_multimodal_description: [Shot 1] 小师妹，浅青色齐胸襦裙…，近景，固定镜头 清晨林间古道… 小师妹 (S2)，眉眼带庆幸，语气温软，说道：<d>[Chinese] 大师兄，昨天晚上，谢谢你救了我。</d>

overall_soundscape: 树叶摩擦的沙沙声与踩在石板路上的声音

non_diegetic_music: N/A
```

`tests/assets.test.mjs` 里有 11 条断言钉住这个结构（`<d>` 标记、soundscape 不含台词、
说话者 ID 跨镜不变、画外音固定短语、**没有否定句**、三段字段顺序）。

**已知偏离**：官方示例的描述文字是英文，我们这边是中文（板子本来就是中文，翻译要额外过模型）。
结构性标记都按官方写死了。

---

## 通道路由：关键帧**也走线上**（推翻了从 DramaClaw 抄来的判据）

原判据（从 DramaClaw 的 `.env` 抄的）是「资产走线上、关键帧走本地草稿」。
**后半条被同镜 A/B 推翻了**（同参考图、同提示词、同分辨率）：

| | 线上 qwen-image-3.0 | 本地 Qwen-Image-Edit |
|---|---|---|
| **构图对得上分镜吗** | ✅ 大师兄在前、小师妹在后 | ❌ **两人位置调换** |
| 人脸 | 真人质感、五官有细节 | 娃娃脸，头身比不对 |
| 服装/道具 | 薄纱层次、剑鞘都在 | 外衫发平，剑变小 |
| 耗时 | 133s | 120s |
| 价格 | **0.24 元/张** | 0 |

**时间几乎一样，只差 0.24 元。** 而关键帧是观众真正看到的画面 ——
14 张 = 3.4 元换整片画质，这个账不用算。

**推广的教训**：「便宜通道用于草稿、贵通道用于成品」这种分工，
**必须验证"便宜通道的成品质量是否够用"**。我抄了判据却没验，结果把最贵的一层（观众看到的画面）
放在了最弱的通道上，还一路以为是"本地算力免费所以划算"。

本地通道保留：探构图、批量试错、线上不可用时兜底（`AIH_KEYFRAME_PROVIDER=comfyui`）。

---

## ⚠ 产物必须核对契约 —— 比例问题的总根因

**三层比例问题，一个都没被任何检查抓到。** 因为它们全都是**静默失败**：
产出照旧，只是形状不对。

| # | 问题 | 根因 |
|---|---|---|
| ① | 全片按 16:9 生成，切竖屏后全废 | `meta.aspect` 是**继承来的默认值**，从没被当成一道题问过 |
| ② | 13 张竖屏关键帧扔掉 68% 像素 | 线上通道**只认 `size`**、本地只认 `ratio`，调用按本地写 → 线上 `size=undefined` 走了默认 `'16:9'` |
| ③ | 成片是 1088×1920 而非标准 1080 | **H3 只能输出 32 的倍数**（`1080/32=33.75`），而配置里做了两次缩放 |

### 为什么整套检查都瞎了

**因为它们在回答"该不该继续"，没有一条在回答"成品符不符合声明"。**

| 机制 | 它回答的 | 为什么不抓形状 |
|---|---|---|
| 闸门 | 该不该往下走 | 只看票 |
| `validate` | 板子结构对不对 | 只看板子 |
| 成本账本 | 花了多少钱 | 只记钱 |
| **审阅图** | 有哪些要看 | **把所有图缩成同一尺寸 —— 正好把比例差抹平** |
| `normalizeSize` | 缩到目标画幅 | **`force_original_aspect_ratio=increase,crop=` 是"强制适配"—— 不匹配时裁掉多余部分替它擦屁股** |

**最刺眼的是审阅图那条**：我为了让它好读把所有图缩成同一尺寸，
**而这个操作恰好抹掉了最该看见的差异**。（并排比关键帧和片段时也犯了同样的错，
是用户逼我重做的。）

### 补上的判据

```js
aspectMatches(w, h, aspect, tol = 0.02)
```

**容差 2% 是量出来的，不是拍脑袋**：

| 来源 | 实际 | 偏离 9:16 |
|---|---|---|
| 标准 | 1080×1920 | 0.00% |
| H3 的 32 倍数对齐 | 1088×1920 | **0.74%** |
| 线上 API 的 "9:16" | 1536×2688 | **1.59%** |
| 线上 API 的 "16:9"（**错的那个**） | 2688×1536 | **211%** |
| H3 节点默认（**错的那个**） | 864×480 | **220%** |

2% 放过两个通道各自的取整约定，**同时把真正的比例错误（差两个数量级）拦死**。

**三个落位点全部接上**（`place()` 带 `expectAspect`）：

- 资产：肖像 `1:1` / 身份图 `16:9` / 场景 = 片子画幅
- 关键帧：片子画幅
- 片段：片子画幅
- **成片：`review()` 里也查**（自检以前只看解码/音轨/时长，唯独没看形状）

**`normalizeSize` 也改了**：不再"强制适配"，改成**先验、只等比缩放**，
不符就打印 `⚠ 画幅不符` 交给下游断言拦。

`tests/orchestrate.test.mjs` 有 **18 条**钉住这个判据 ——
包括拿**这次真出过的两种错产物**（`2688×1536` 和 `864×480`）去撞，必须都被拦下。

---

## 采样步数：**默认 4 步，25 步要用户发话**

**这条是用户定的规矩，我照着执行。**

```bash
node src/render.mjs <board> --workspace <WS> --stage clips        # 默认 4 步（--fast）
node src/render.mjs <board> --workspace <WS> --stage clips --hq   # 25 步，**要用户明确要求**
```

### 为什么反过来

我原来把 **25 步设成默认**，理由是"本地算力免费、该换质量"。**这个理由站不住**：

- 25 步和 4 步的差别**我一次都没对照过** —— 等于用"没验证过的质量"换"实实在在的时间"
- 结果是**每次试错都要等 25 步**，而这一轮试错次数极多（光片段就重跑了三轮）

**现在的规矩（按用户原话）：**

> 先用 4 步生图。视频片段或生成结果没问题了。听我指令再考虑是否用 25 步。

1. **默认 4 步**，先把链路和效果验通（实测快 **7 倍**：s04 用 4 步 **11 秒**，25 步约 75 秒）
2. **看过了、确认没问题，用户说了才**上 `--hq`
3. `--hq` 只影响**采样步数**，不影响分辨率

### `--fast` 对视频不只是减步数

```python
# gen.py 视频分支
steps = args.steps if args.steps is not None else (4 if args.fast else 25)
cfg   = args.cfg   if args.cfg   is not None else 1.0
lora  = graphs.M_H3_LORA_4STEP if args.fast else None
```

`--fast` 还挂一个加速 LoRA：

```
minimax_h3_fl2v_turbo_4step_v1.0_768p_comfyui_bf16.safetensors
                                  ↑↑↑↑↑
                                 名字里写着 768p
```

**而我们是按 1088×1920（≈1080p）出的 —— 分辨率是否匹配、有没有画质损失，未验证。**

### 耗时跟什么走

**跟帧数成正比，不是"每镜固定几分钟"。**

```
实测干净单进程：**1.33 秒/帧**（1088×1920）
并发三进程时：7.92 秒/帧（慢 6 倍 —— 那是我自己造成的）
```

逐镜估算要用 `帧数 × 1.33 秒`，`帧数 = 17k+5` 网格上对齐的结果。
**别再用"平均 X 分钟/镜"** —— 这一轮我用这个错误的估法估了三次，次次错。

---

## 生成片段：**生一条，检查一条**（用户定的规矩）

> 用户原话：「一段一段跑。别一下发一大堆，结果发错了。生一条检查一条。」

**不要一次把整批镜头提交进去。** 一次只跑一个镜头，**验完再跑下一个**。

### 为什么

我一次性跑整批（还并发跑了 3 批），结果是：

```
12 个生成 → 只活下来 4 个
  5 个比例错（864×480，并发进程加载了旧代码）
  3 个重复（进程抢文件，板子记录被互相覆盖）
浪费 67%
```

**一旦整批提交，错误的参数会在整批里复现，而你直到最后才发现。**
一个一个跑，第一个就能暴露问题，后面 13 个不用白烧。

### 怎么做

```bash
# 一个镜头一条命令 —— 不是 --shots s05,s06,s07
node src/render.mjs <board> --workspace <WS> --stage clips --shots s05

# 验完这个，再跑下一个
node src/render.mjs <board> --workspace <WS> --stage clips --shots s06
```

**每一条跑完必须检查：**

1. **画幅对不对**（`probeWxH` 量实际值，不能靠"应该没问题"）
2. **文件真的落位了**（板子上有 `clip`，磁盘上有文件）
3. **时长/帧数符合预期**
4. **内容对不对**（抽帧开原图看，**不要在缩图上判断**）

**任何一条不过 → 停下来查，不要继续跑下一个。**

### 并发是绝对禁止的

单实例锁会挡住第二个进程（`exit 4`）。**不要用 `Start-Job`、`Start-Process`、多开终端绕过它。**

### 要停的时候

**只杀 `render.mjs` 进程，让当前这一镜跑完。**
或者发**一次** `/interrupt` 然后等。

> ⚠ **绝对不要连发 `/interrupt`。** 我连发了 5 次去停一个正在算的 GPU，
> 结果把 CUDA 上下文搞成 `illegal memory access`，**整个 ComfyUI 崩了**。
> 量化模型（int8）对这种中断尤其脆弱。

---

## 镜与镜的衔接：**首尾帧夹逼（fl2v）**

> 用户原话：「有的时候在生第二个视频的时候为了保障连贯性，这种首尾帧模式也是需要的。」

**相邻两镜如果是同一段连续的动作 / 同一片空间，就该用 fl2v 接上，而不是各跑各的。**

### 机制

```
本镜的首帧  ──fl2v──▶  下一镜的首帧
                         ↑
                    本镜的尾帧 = 下一镜的首帧  →  接上就是无缝的
```

**方向是个坑**（踩过）：不是"上一镜的尾帧 → 本镜的首帧"，而是
**"本镜的首帧 → 下一镜的首帧"**。所以标记打在**前一镜**身上：

```jsonc
{ "id": "s06", "transition": { "type": "last_frame_first" } }   // s06 走去 s07 的起点
{ "id": "s07", "transition": { "type": "last_frame_first" } }
{ "id": "s08", "transition": { "type": "cut" } }
```

`flfPlan()` 据此把 mode 从 `i2v` 切成 `fl2v`：

```js
const chained = shot.transition && shot.transition.type === 'last_frame_first';
const last = shot.last_frame || (chained && next ? next.first_frame : null) || null;
if (chained && first && last) mode = 'fl2v';
```

### 什么时候用 / 不用

| 用 `last_frame_first` | 用 `cut` |
|---|---|
| 同一段动作被切成两镜 | 换了地点、换了时间 |
| 对话正反打（视线接得上） | 刻意的时间跳跃 |
| 一个运镜拆成两段 | 两个独立的画面 |
| **下一镜的首帧已经存在** | 下一镜还没首帧 |

> **声明了 fl2v 但下一镜还没有首帧 → 退回单帧起步，并打印原因。**
> 所以关键帧要先出齐，fl2v 才有意义。

### 怎么验它真的接上了

用检查工具把两镜的首末帧抽出来对：

```bash
node tools/inspect.mjs clips/s06.mp4 --aspect 9:16 --first-last
node tools/inspect.mjs clips/s07.mp4 --aspect 9:16 --first-last
# 然后比 clips/s06_last.png 和 clips/s07_first.png —— 应该几乎一样
```

**不要凭"我设置了 fl2v"就认为接上了。** 要看到两张图。

---

## 检查产物：用工具，不要现场拼命令

```bash
node tools/inspect.mjs <video|image> [选项]
```

| 选项 | 说明 |
|---|---|
| `--frames N` | 抽几帧（默认 6） |
| `--height px` | 每帧高度（默认 360，**自动偶数对齐**） |
| `--out path` | 抽帧图输出路径 |
| `--aspect 9:16` | 核对画幅（不给就只报告） |
| `--tolerance 0.02` | 画幅容差 |
| `--first-last` | **额外导出首帧/末帧**（做衔接核对要用） |

输出：

```
检查 s01.mp4
────────────────────────────────────────────────────────────
  尺寸      1088×1920  @ 24fps
  时长      3.042s
  帧数      73
  音轨      有
  画幅      期望 9:16 → ✓ 通过（偏离 0.74%，容差 2.0%）
  解码报错  0 行 ✓
  响度      mean -41.3 dB / max -20.4 dB
  抽帧      6/6 帧 ✓
  抽帧图    …/s01_frames.png
  首帧      …/s01_first.png  ✓
  末帧      …/s01_last.png  ✓
────────────────────────────────────────────────────────────
  ⚠ 电平偏低（-41.3 dB）—— 如果这镜有台词，就是有问题
✓ 全部通过
```

**为什么要有它**：我每次现场拼 ffmpeg 抽帧都翻车 ——
`scale=300:533` 高度奇数报错、逐帧高度差 2px 被 `hstack` 拒绝、
PowerShell 循环里 `-ss` 拼错只写出第 1 帧。
**检查产物是高频动作，就该是个钉死的工具。**

**退出码 0 / 1**，可以直接串进流水线。

---

## 分辨率：**照官方文档用 768×1344，不要自己扫**

> **这一节是血的教训。** 我绕开文档自己扫了一遍"H3 接受哪些尺寸"，
> 挑了最大的 1088×1920（2.09MP，官方参考的 **5.2 倍**），然后一路把"慢"和"崩"
> 归因到长度上，烧掉了一整晚。
>
> **`comfy-studio/references/capabilities.md` 里全写着**：实测输出 864×480、
> 每种模式的实测耗时（`--fast` 5 秒片：i2v 40.2s / fl2v 39.8s）、上限 15 秒、
> `--fast` 的 LoRA 是 768p。**节点默认也是 864×480。我一次都没打开。**

### 实测对照（9:16，328 帧 = 14 秒，4 步，--fast）

| 尺寸 | 像素 | 耗时 | 4 刀切对 | 字幕 | 画面 |
|---|---|---|---|---|---|
| 480×864 | 0.41 MP | **98 s** | ✅ | ❌ **烧字幕** | 好 |
| **768×1344** | **1.03 MP** | **269 s** | ✅ | ✅ **无** | **好** |
| 1088×1920 | 2.09 MP | **973 s** | 部分 | ✅ 无 | ❌ **尾段雪花** |

**用 `768×1344`** —— 它是那个 turbo LoRA 的原生 768p，而且是 32 的倍数
（768/32=24，1344/32=42），H3 接受。交付到 1080×1920 只需放大 1.4 倍。

### 三条被实测钉死的事实

**① 耗时随长度超线性涨，随分辨率约线性涨**

```
同一 328 帧：  480×864 → 98s    768×1344 → 269s   1088×1920 → 973s
同一 1088×1920：124 帧 → 220s   328 帧 → 973s
```

**所以别拿"每帧多少秒"线性外推**（我推过三次：7 分钟/镜、1.33 秒/帧、0.196 秒/帧，全错）。

**② 低分辨率会烧字幕，高分辨率不会**

```
 480×864   →  烧字幕（两次运行、两种提示词结构，都有）
 768×1344  →  无字幕
1088×1920  →  无字幕
```

**分界线在 480 和 768 之间。原因不明**，但事实稳定。
官方指南 4.5 节只说"画面上的文字要用英文双引号写"——**没说怎么让它不写**。
节点里**没有**任何 ffmpeg 烧字代码（搜过 `subtitle` / `.srt` / `drawtext`），
所以**是模型自己画的**。

> ⚠ 别再拿"字幕"下结论而不看原片。我早前扫过旧片段得出过"H3 不烧字幕"，
> 写进过正本；后来又看到它烧。**两次都是真的 —— 取决于分辨率。**

**③ 长镜合并能成，但要配对分辨率**

```
768×1344  × 328 帧  →  269 秒，4 刀全对，无崩坏   ✅
1088×1920 × 328 帧  →  973 秒，尾段雪花          ❌
```

**"一镜到底"这条路是通的** —— 前提是尺寸对。

### 尺寸参数怎么传

```bash
node tools/unit.mjs <board> --direction <dir> --unit u1 --size 768x1344
```

**工具默认就是 `480x864`**（最便宜，用来验证）。**出片前会打印尺寸。**
**验证完一定从 ComfyUI 工作流里核对实际值** —— 我用 PowerShell 做字符串替换时
漏过一次，连跑两次都是 1088×1920 还浑然不觉：

```powershell
$h = Invoke-RestMethod "http://127.0.0.1:8188/history?max_items=1"
# 看 MiniMaxH3Director 节点的 width / height / total_frames / steps
```

### 硬上限：**768×1344 下 328 帧必过，345 帧必崩**

**这条是实测出来的，两个变量都隔离过了：**

```
u1 内容  328 帧  →  269 秒  跑通
u1 内容  345 帧  →   27 秒  崩     ← 同样的内容、同样的首帧，只多 17 帧
u2 内容  345 帧  →   55 秒  崩
```

**所以跟内容无关，就是帧数。** 崩溃形式：

```
Error running sage attention: CUDA error: an illegal memory access was encountered
, using pytorch attention instead.
```

**而 `attention_sage` 里其实有 fallback（`attention.py:761-771`），但它救不了** ——
CUDA 非法访问会**销毁上下文**，之后任何 CUDA 调用都失败，进程直接死。

```
 328 帧 = 13.67 秒   ✅
 345 帧 = 14.375 秒  ❌
```

**文档说 `--duration` 上限约 15 秒 —— 但在 768×1344 + SageAttention 下，实际安全上限是 13.6 秒。**

> SageAttention 是用 `--use-sage-attention` 开的（`cli_args.py:150`），**不是默认**。
> 但这台机器上日志里有 `Using sage attention`，而**绘世启动器的 `data.json` / `preference.json` 里找不到这个键** ——
> 它藏在启动器的专家设置里。**关掉它也许就能用满 15 秒，未验证。**
> 而且它今晚崩了四五次（包括连发 `/interrupt` 的那次），**在这块卡上本来就不稳。**

### 超长时怎么办：**等比缩，不要砍尾巴**

```bash
node tools/unit.mjs <board> --direction <dir> --unit u2 --fit 13.6
```

`--fit` 会**按比例缩放整条时间线**（切点、每镜时长一起缩），
**保持镜头之间的相对节奏**，只是整体紧一点。

**不要砍尾巴** —— 那会把最后一镜截断，破坏导演设计的镜头比例。

---

## 去硬字幕：**后期做，别在生成端较劲**

**✅ 已落实。** 工具：`tools/desub.mjs`。

```bash
node tools/desub.mjs <视频> [--out <路径>] [--top 0.71] [--bottom 0.87] [--mode sttn-det]
```

**实测**：768×1344 的 13.6 秒片段 **21 秒**；67 秒整片 **66 秒**。**无分辨率损失。**

### ⚠ VSR 输出的是 mpeg4，必须重编码

`
VSR 输出   codec = mpeg4 (Simple Profile)   ← 很多播放器/浏览器打不开
应该输出   codec = h264 (High) + aac 44.1k + faststart
`

**我（AI）当时没查编码就把文件发给用户，用户回了一句「打不开」。**
fmpeg 能解码 ≠ 用户能播 —— **交付前必须查 codec_name，不是只看"文件存在"。**

`tools/desub.mjs` 现在会自动重编码（-crf 18，11 秒跑完 67 秒片），并删掉那个打不开的原件。

### 为什么必须后期做

H3 **不稳定地烧字幕** —— 同尺寸、同提示词，u2/u3 稳定出，u1/u4/u5 稳定不出。
**加 `on_screen_text: none` 只对一部分有效。**

**裁切也不行**：字幕在画面高度约 **74%–80%**，裁掉要损失 26%，
768×1344 → 768×994（比例 0.57 → 0.77），竖屏构图就废了。

### 怎么做的

`YaoFANGUK/video-subtitle-remover`（12.9k stars）的 Docker 镜像：

```bash
docker run --rm --gpus all -v "<项目目录>:/data" \
  eritpchy/video-subtitle-remover:1.4.0-cuda12.6 \
  python backend/main.py -i /data/in.mp4 -o /data/out.mp4 \
  -c 950 1170 0 768 --inpaint-mode sttn-det
```

`-c ymin ymax xmin xmax` 是字幕带坐标（**我们量出来是画面高度的 71%–87%**）。
算法：`sttn-det`（真人视频，快）/ `lama`（图片最好）/ `propainter`（运动剧烈）。

### ⚠ Docker 的代理坑（这里卡了很久）

**Docker Desktop 的代理配置可能是陈旧的。** 这台机器上它写着 `127.0.0.1:7890`，
而 Clash 早换到 `52740` 了 → `docker pull` 直接失败：

```
failed to resolve reference ... connecting to 127.0.0.1:60999:
No connection could be made because the target machine actively refused it
```

**修法**：

1. **退出 Docker Desktop**（运行中改配置会被覆盖回去）
2. 改 `%APPDATA%\Docker\settings-store.json` 里的
   `OverrideProxyHTTP` / `OverrideProxyHTTPS` → 当前真实代理端口
3. 重启 Docker Desktop

**怎么找真实端口**：`HKCU:\Software\Microsoft\Windows\CurrentVersion\Internet Settings`
的 `ProxyServer`（Clash 用的是随机端口），或者看代理软件的设置。

---

## 下一轮开工前先看这四条（用户 2026-09-15 定的）

### ① 去字幕 —— ✅ **已落实**（见上一节，`tools/desub.mjs`）

### ② 关键帧美感 —— 换「绘梦」的 image2 接口

现在关键帧走**线上 qwen-image-3.0**（0.18 元/张，1080×1920）。
用户说绘梦有个 image2 接口**效果可能更好**，作为**下一个剧本的重点**试。

> 关键帧质量决定后面所有东西的上限 —— 视频是"从首帧长出来的"，
> 首帧不好，视频再好也救不回来。**这个投入比调视频参数值。**

### ③ 配音最后一段有瑕疵（未查）

用户听出来的。**还没定位是哪一句、什么瑕疵。**
下一轮先问清楚：是**音色跳**、**断句怪**、还是**和口型对不上**。

### ④ 另一个值得看的 H3 工具

```
xm6018924/BSAI-ComfyUI-H3-Film-Factory
"asset library, storyboard clip management, subtitle rendering,
 media combining, and contextual frame extraction"
```
**H3 专用影视工具箱**，2026-09-15 还在更新。下一个剧本前值得看一眼。

---

## ⚠ 诊断工具自己的坑（这一晚误判了四次）

**这一段比技术参数更重要 —— 因为它让我把"我的工具出错"报成了"环境坏了"，
让用户白白重启了四次 ComfyUI。**

### ① PowerShell 管道里的 `Select-Object -First N` 会杀死上游进程

```powershell
# ✗ 匹配到 N 行就掐断管道 → node 进程被杀 → 报成 ConnectionResetError
node tools/unit.mjs ... | Select-String '出片|✓' | Select-Object -First 5

# ✓ 先收全，再挑
$out = node tools/unit.mjs ... 2>&1 | Out-String
($out -split "`n") | Where-Object { $_ -match '出片|✓' } | Select-Object -First 5
```

**实测**：同一个 u2 命令，带 `-First` 时"0 秒失败"，不带时**跑满 267 秒成功**。

而且它会**伪造出一个很像真的错误**：`ConnectionResetError: 远程主机强迫关闭了一个现有的连接`
—— 因为 node 被杀了，它到 ComfyUI 的连接就断了。**看起来完全像 ComfyUI 崩了。**

> **规则：跑长任务的命令，绝不在管道里 `Select-Object -First`。**

### ② 端口通 ≠ ComfyUI 就绪

```
22:50:09  开始加载 custom_nodes
22:50:14  注册 HTTP 路由      ← 端口从这里就通了
22:50:21  MiniMaxH3Director 加载完   ← 节点到这里才可用
```

**我在 22:50:15 提交，gen.py 被立刻拒绝，0 秒退出 —— 我报成"又崩了"。**

用 `node tools/wait-ready.mjs`（轮询 `/object_info/MiniMaxH3Director`，而不是只看端口）。

### ③ 不要拿单个文件下结论

**这一晚至少七次**，每次都是同一个错误：抽一两个文件（有时只有一帧），
就对外说"**X 是 Y 决定的**"，然后被用户一句"你确定"打回来。

| 我的结论 | 真相 |
|---|---|
| H3 不烧字幕 | **取决于采样，不稳定** |
| 768×1344 不烧字幕 | 同一个尺寸另一个文件就有 |
| u2 崩是帧数问题 | 是我管道杀的进程 |
| 每帧 0.196 秒可以外推 | 长镜完全不成立 |
| 合并长镜更快 | 慢 4.4 倍 |
| 1088×1920 可用 | 5.2 倍超规格，又慢又崩 |

**规则：说"X 决定的"之前，至少要有两个对照样本，而且要看原片（不是缩图）。**

### ④ 别信自己扫出来的"可用尺寸"

`comfy-studio/references/capabilities.md` 里写死了 **864×480**，节点默认也是它。
我绕过去自己扫，扫出"H3 接受哪些尺寸"，**然后挑了最大的** ——
"接受"不等于"该用"。**文档里有实测耗时表，我一次都没打开。**

---

## 契约的核心设计

### 1. 身份与造型分离（这是本工程最重要的一条）

```jsonc
"characters": [        // 身份层：一个角色全片只有一张脸
  { "id": "xie", "age_group": "youth", "face_prompt": "纯面部特征，禁写服装", "portrait": "…" } ],

"identities": [        // 造型层：一个角色可以有多套
  { "id": "xie_emperor", "character": "xie", "name": "龙袍",
    "appearance_details": "服装、配饰、发型造型（禁写动作表情）",
    "costume_image": "…",   // 服装参考图：脸取自 portrait，衣取自这一张
    "sheet": "…" } ],       // 身份图：一次生成的 4 面板参考表

"shots": [ { "cast": ["xie_emperor"] } ]   // 镜头引用造型，不是角色
```

**为什么必须拆**：把 `wardrobe` 挂在角色上，等于假设全片只有一套衣服；剧本一换装，结构就崩。
另外 `age_group` 是**受控枚举**（child/youth/middle/elder），不是自由文本 ——
写「少女」「青年」这种跨十年的词，等于把年龄交给模型抽签（实测因此把成年角色画成幼儿园小孩）。

### 2. 标记协议

出场人物在 `prompt` 里用 `{{identity_id}}` 内联，道具用 `[[prop_id]]`。校验器检查：

- 标记必须来自**本镜的 cast / props 菜单**
- cast 里的身份在 prompt 里**没有标记** → 警告（绑定不明确）
- 标记是**绑定**，不是给人读的：渲染器拼最终提示词时先把它换成该身份的 `appearance_details`

### 3. 禁止含糊

`prompt` / `action` 里出现「或者」「二选一」→ **直接报错**。
含糊的画面根本没法渲染 —— 这一条一个正则就够，但能拦掉一整类废图。

### 4. 资产分工

| 资产 | 走哪 | 通道 | 为什么 |
|---|---|---|---|
| 角色 `portrait`（脸锚点） | **线上** | `bl image generate`（qwen-image-3.0） | 数量少、要一致性 |
| 角色 `sheet`（4 面板身份图） | **线上** | `bl image edit`（吃本地路径） | 多参考合成，本地做不好 |
| 场景 `master` / `reverse_master` / `spatial_layout` | **线上** | 同上 | 同上 |
| 道具三视图 | **线上** | 同上 | 同上 |
| 关键帧（批量草稿） | **本地** | ComfyUI（Qwen-Image-Edit） | 数量多、要便宜快 |
| 关键帧（hero 镜 / 反复不合格） | **↑ 升级线上** | `bl image edit` | 判据：数量少、要质量 |

**为什么是 `bl` 而不是绘梦 image2**：绘梦只吃**公开 URL**（DramaClaw 得先传 Cloudinary），
`bl image` **直接吃本地文件路径**，而且 `--image` 可重复（多图合成）、`--n` 最多 6（正好当候选池）。

**调 `bl` 的三个坑（都实测过）**：
1. `bl` 是 **PowerShell 脚本**，而这台机器上的 "pwsh" 其实是 **Windows PowerShell 5.1**（PS 7 没装）
   → 必须用 `powershell.exe` 全路径调，别指望 `pwsh`。
2. **PS 5.1 会吃掉参数里的 `"`** → 提示词一律**写临时文件**由脚本读进来；
   `providers/bailian.mjs` 的 `sanitize()` 把 `"` 换成全角，**绝不把提示词拼进命令行**。
3. `--watermark` 默认 **true**，必须显式关掉，否则资产图上带水印。

**不做草图（线稿→上色）两遍。** 那一套换来的构图控制，代价是每镜两遍生成 + 两轮人工挑选，
和「减少人工介入」的目标直接冲突。我们改用**更强的线上模型 + 多参考图合成**替代它。

### 5. 必需的外观属性由代码兜底

**两次栽在同一个坑上**，所以立成规矩：

| 属性 | 谁拥有 | 不兜底的后果（实测） |
|---|---|---|
| `age_group` | schema 受控枚举 | 写「少女」→ 成年角色被画成**幼儿园小孩** |
| **族裔** | `assets.mjs` 的 `regionAnchor()` | `face_prompt` 里没有族裔 → 古风仙侠主角被画成**白人** |

**规律**：只要一个外观属性是"必需的"，就不能指望模型自觉 —— 它必须有代码拥有者。

**第三个栽在这上面的是「风格」。**

肖像配方里构图约束写了七条（正面 / 脸占 60-70% / 灰底 / 素色上衣 / 禁服装细节…），
**风格一个字都没写**。因为怕成片色调污染锚点，我故意不带 `style_prompt` ——
结果把"这是真人实拍"也一起扔了。

一部 `style: realistic` 的写实剧，小师妹长成了**卡通人**。
用户原话：「写实剧里的小师妹像卡通人啊」。

修法：`assets.mjs` 的 `styleAnchor(board)` —— 给的是**媒介/质感**（写实实拍 vs 2D 赛璐璐），
**不是色调**。色调仍然归 `style_prompt`，而 `style_prompt` 依然不进肖像。

| 属性 | 谁拥有 | 不兜底的后果（实测） |
|---|---|---|
| `age_group` | schema 受控枚举 | 成年角色 → **幼儿园小孩** |
| **族裔** | `regionAnchor()` | 古风仙侠主角 → **白人** |
| **风格** | `styleAnchor()` | 写实剧角色 → **卡通人** |

**修好风格锚点之后暴露出的下一件事**：`appearance_details` 还是占位符「小师妹的服装（待补）」，
所以服装是**模型自己选的**。它选的是白色的交领上衣 + 淡青比甲 + 灰百褶长裙 ——
是**新中式/古装**没错，但在古风仙侠的剧里偏**素净**，不像该有的飘逸感。

> **这里我犯过一个错，记下来。** 我从一张 **760×428 的缩图**（原图 2688×1536）里
> 看到"白 + 灰"，就断言"模型给她穿上了现代便装"。**打开原图才发现那是交领+比甲的汉服元素。**
> 又一次**拿降级的观测下结论**（上次是"没声音"那次，我信了 `volumedetect` 的数字）。
> **看细节就去开原图，别在缩图上做判断。**

所以校验器现在会**点名占位符**：

```
warning  characters: "c_xxx" 的 face_prompt 是占位符 —— 长相会由模型自己编，出图前先填上
warning  identities: "c_xxx_default" 的 appearance_details 是占位符 —— 服装会由模型自己编
```

**这就是闸门存在的意义**：这一串问题本该在「资产确认」那一步就被你拦掉，
而不是等成片出来才发现「画质不敢恭维」。

### 6. 参考图会"传染"它身上的东西

**肖像锚点身上穿的那件素色上衣，会被身份图一并继承过去** —— 实测：4 个面板里只有脸是对的，
身上还是那件 T 恤，文字里的服装描述被完全忽略。

修法是**在指令里显式切断**：

> **参考图只用于锁定长相（脸型、五官、发型）；参考图里的衣着必须完全忽略，
> 四个面板的服装以上面那段服装描述为准**

加了这一句，同一份配方同一张参考图，身份图的服装立刻正确（白色长衫 + 深蓝腰带）。

DramaClaw 的 `_costume_reference_block` 说的是同一件事（「脸取自第一参考、衣取自第二参考」）——
**更强的做法是给一张服装参考图；退一步，至少要在文字里显式声明"忽略参考图的衣着"。**

### 7. 候选池 + 逐维打分（不靠人挑）

`--n N` 每项抽 N 个候选，**本地视觉模型**（`qwen3.5:27b`，带 vision）逐维打分择优：

```
判据（抄自 DramaClaw）：face_score ≥ 7 AND clothing_score ≥ 7 AND 无 critical
身份图额外：面板必须 ≥ 4、不许把文字烧进画面
肖像额外：族裔对、正面、背景干净
```

**挑选是"先看过不过，再看总分"** —— 否则会出现"脸 3 分但构图 10 分"的废图当选。
全部不过时返回分最高的那个并标 `provisional`：**别让流程卡死，但要让人知道这是勉强用的**。

候选池留在 `shots/pool/<id>_<slot>/`（含 `score.json`），随时可人工复核。

**打分跑本地的三个理由**：免费（可以反复抽）、打分只是"看一眼给个数"不需要最强模型、
候选动辄几十张调线上会烧钱。缩图到 768 边长后约 20-45 秒一张。

**已知的局限（别自欺）**：绝对打分**偏宽松** —— 三个候选常常都给 9 分，区分度不高。
真正在干活的是**硬条件**（面板数、有没有烧字、族裔、正面、critical）和**单次抽卡的偶发崩坏**
（实测抓到过一帧两张脸糊成一个双头）。DramaClaw 的做法是"分差大就自动选，分差小就再做一次
成对比较"—— 成对比较这一步我们还没做，是下一步该补的。

**这条回路真的在干活** —— 它抓到过一个我没当回事的缺陷：

```
身份图三轮演进：
  ① 4 面板，但 Panel2/3/4 是 T 恤     ← 肖像的"素色上衣"被继承
  ② 服装对了，但烧进了"角色设定图/年龄/门派/服装配色"排版   ← has_baked_text=true，一票否决
  ③ 无文字、服装一致、干净              ← 现在过线
```

**② 那一轮是我肉眼看过却没当回事的**：那张图要当参考图喂给关键帧，烧进去的字会跟着污染画面。

### 8. 关键帧：本地跑，但**别忘了 `--fast`**

关键帧走本地 ComfyUI（便宜、量大）。两个提速点，**第二个是最贵的疏忽**：

| 疏忽 | 代价（实测） |
|---|---|
| 忘了传 `--fast` | **121 秒/张** —— Lightning LoRA 是 4 步 + CFG 1.0，不传就是 20 步 + CFG 4.0 |
| 参考图不降采样 | 拿 2688×1536 的身份图直接喂 —— 参考图不需要那么大，1024 长边足够 |

两个都修完：**121 秒 → 16 秒（7.5 倍）**，14 镜从 25 分钟降到 3.5 分钟。

另外 `gen.py` 的 `--batch` **只对 t2i/music 有效，对 edit 无效** —— 抽候选只能循环换种子。

### 9. 编排：三条顺序问题

**① 时长由音频反推，不是拍脑袋写死。**
我们原先"先定 5 秒、再想办法把话塞进去"。实测一跑就露馅：s05 估 9 秒，
**配音实测 12.14 秒** —— 按原时长会把台词截断。正确顺序是**先出配音、量出秒数、再定画面时长**。

**② 首尾帧方向反过来。**
我们原先：拿**上一镜的末帧**当本镜首帧 → 链式的，一镜废了后面全废。
DramaClaw：拿**下一镜的首帧**当本镜尾帧（`fl2v`）→ 先出全部首帧，再两两夹出中间的运镜。
后者不但容错，画面还真的"走到"下一镜的起点。下一镜还没首帧时**诚实退回 i2v 并说明原因**，不硬来。

**③ 帧数必须落在 H3 的 17k+5 网格上。** 这是引擎约束，`snapFrames()` 负责对齐。

**音色也是"必需属性"**：`characters[].voice` 填了 voice ID 就用它；
没填时按名字/脸描述里的性别线索兜底（实测服务端的 `--instruction` 在 `cosyvoice-v3-flash` 上
**不支持**（报 428 InvalidParameter），情绪改由**确定性的语速/音高映射**表达 —— `emotionToProsody()`）。

### 10. H3 会把对白烧成字幕 —— 压不住，只能裁

**实测结论**：H3 会把 `overall_soundscape` 里的对白**当画面文字渲染出来**，而且**会写错字**
（「你果然是个木头」糊成「你然是个头」）。

两种措辞都试过，**都没用**：

| 试法 | 结果 |
|---|---|
| 否定句「画面中不出现任何字幕、文字、标题或水印」 | ❌ 字幕还在，只是换了句词 |
| 肯定句「画面干净，只有人物与林间环境」 | ❌ 字幕还在 |

**教训**：否定词写在"要画什么"那一段里会被当成内容；但对 H3 来说，只要对白在提示词里，
它就会往画面上写字 —— 这是模型行为，提示词层面压不住。

**解法是后期**：拼接时裁掉底部 13% 再拉回原尺寸（`crop=iw:ih*0.87,scale=...`）。
`--keep-caption` 可以保留硬字幕（有些平台就是要）。

### 11. 成片必须自检 —— **"文件存在"不等于"成片能看"**

**这条是血的教训，而且是被用户的耳朵发现的。**

第一次出片时我报的是「成片 → final.mp4，成功」。**音轨其实是坏的。**
根因：无台词的镜头保留了 H3 自己 32000Hz 的音轨，有台词的 12 个换成 TTS，
**混着 `-c copy` 拼接 → AAC 比特流废掉**。播放器能读出时长（88.7s），
但解码器满屏 `channel element ... is not allocated`，听上去就是**没声音**。

**而我当时用 `volumedetect` 量出「−20 dB，有声音」—— 那是解码器在硬啃垃圾数据。**

修了两处：
1. **逐段统一音频参数**（`-ar 44100 -ac 2 -b:a 192k`），不给 `-c copy` 拼错的机会
2. **把 H3 的环境音混进来**（`amix`，H3 0.35 / TTS 1.0）—— 只 map TTS 会把 H3 的音整条丢掉

外加一个坑：`apad`（无限音频）+ `amix` + `-shortest` 会**死锁**（实测卡住，ffmpeg CPU 停在 0.33 秒）。
改成显式 `-t <视频时长>`，不让 ffmpeg 去猜。

**所以 `assemble` 跑完必须过 `src/review.mjs`，不过就是 exit 1：**

| 检查 | 判据 |
|---|---|
| **完整解码一遍数报错** | 必须 0 行 —— **这一条一个就能抓住上面那个 bug** |
| 音轨存在且不是死的 | mean > −50 dB |
| 时长对得上时间轴 | ±3 帧 |
| 抽 5 帧查亮度 | 不能全是黑或全白 |
| 字幕条数 | 与时间轴核对 |
| 警告项 | 响度偏低、峰值削波 |

验收是**负例驱动**（`tests/review.test.mjs`，13 项）：造全黑无音的、静音的、
时长不符的、**把 AAC 数据砸烂的**，四种病**都必须被拦下**。

### 12. 成本账本 —— 花了多少，当场知道

做完那部片子花了约 **3.9 元**，而我**一次账都没记**，事后只能翻会话重建。
重建之后才发现：**其中 1.44 元（37%）是我自己的 bug 造成的重出。**
**不记账，这种浪费就一直隐形。**

```
每次线上调用 → ledger.add() 当场记一笔 → 写进 story2video/costs.json
跑完打一张表：按模型 / 按轮次（首轮 / 重出 / 局部）
```

| 判据 | 值 |
|---|---|
| qwen-image-3.0 | 生成 0.18 元/张，参考图输入 0.02 元/张 |
| cosyvoice-v3-flash | 1 元/万字符 |

来源是本地那份百炼模型市场数据；**价格会变，用之前核一眼**，或用 `AIH_PRICE_<模型>` 覆盖。

**三条设计原则**：
1. **调用当场记**，不在事后估 —— 参数（几张图 / 几个字符）来自真实调用
2. **本地调用也记**，金额 0 —— 要看得见工作量，不能只看得见钱
3. **不认识的模型会喊出来**，不悄悄按 0 算然后给一个看着很美的合计

**这不是账单**，是"我这次花了大概多少"的即时反馈。真实账单要
`bl auth login --console` 之后 `bl usage summary`。

### 13. 音轨用 H3 自己的 —— **不要再叠 TTS**（已实听确认）

**H3 是音视频联合建模的：对白写进 `overall_soundscape` 之后，它自己就把人声生成出来了。**

实测一个带台词的片段，音频包络：

```
P10 −81 dB   P90 −10 dB   波动 27 dB
```

**P90 到 −10 dB 的爆发 + P10 掉到 −80 dB 的静默** —— 这是语音的签名（音节之间的停顿），
不是环境底噪。对比：纯环境音的片段波动明显更小。

所以**再叠一层 TTS 就变成两个人在说同一句话**。默认只留 H3：

```powershell
--stage assemble              # 默认：H3 自己的声音（人声+环境音，天然同步）← 实听正常
--stage assemble --tts        # 换成 TTS 配音（H3 念得听不清时才用）
--stage assemble --mix        # 叠两层（会听到两个声音，只在排查时用）
```

**TTS 仍然要跑** —— 它的秒数用来反推画面时长（见第 9 条），只是不再混进音轨。

> **这条是靠耳朵定下来的，不是靠测量。** 我一路量电平、量解码、量包络，全都"正常"，
> 而用户听到的是"没声音"。翻车链条：
> ① AAC 比特流被 `-c copy` 拼废（真·没声音，已修）
> ② 修好后我**自作主张叠了 TTS** —— 两个声音说同一句
> ③ 改成只用 H3 的原生音轨 → **实听正常**
>
> 教训：**能测量的东西不等于能听见的东西。** 音轨这类主观项，最终判据是耳朵。

### 14. 响度要归一化

`amix` **默认把每路音量除以路数**（两路就减半），要显式 `normalize=0`。
混完再过一遍 `loudnorm=I=-16:TP=-1.5:LRA=11` —— 手机扬声器小，−23 dB 是真的听不清。

### 15. 工作区与产出路径

- **`WORKSPACE` 不能用 `process.cwd()` 想当然** —— 实测从工程目录跑 render.mjs，
  7 张资产全生成到工程里去了。现在必须显式 `--workspace` 或 `AIH_WORKSPACE`，
  而且**工作区里没有 `story2video/` 就直接拒绝运行**。
- **绝不把 URL 写进契约**：`bl` 返回的 OSS 链接带 `Expires`，**会过期**。
  拿不到本地文件时**下载回来**；`toRelative()` 遇到 URL 直接抛错。
- **产出认领要认 stdout 的 `saved`，不能只比目录快照** —— 覆盖已有文件时名字没变，
  比快照会误判成"没有产出"（实测踩过，然后错误地退回了 URL）。

---

## 两条编译路线（这是本工程最关键的一次取舍）

```
from-story   自由编译：把剧本丢给模型"写一版分镜"
             → 模型顺手改写台词，12 分钟 2 轮，靠提示词求它别改

literal      逐行模式：代码切行 → 代码分组 → **代码搬台词**
             → 台词从头到尾不经过模型，不到 1 秒，逐字保真是构造保证
```

**成品剧本走 `literal`。** 「逐字保真」不该是靠提示词求来的，该是靠**架构**保证的 ——
模型压根不负责输出台词，它就没机会改。

| | 代码负责 | 模型负责（可选） |
|---|---|---|
| `literal` | 切行 / 分组分镜 / **台词原文** / 出场造型 / 时长估算 | 润色 prompt、补 audio、选景别运镜 |
| `from-story` | 契约校验 / 补强 | 人物、造型、场景、道具、镜头，**全部** |

## 命令

```powershell
$H = "D:\DeepSeek\ai-images-harness"

# 成品剧本（推荐）：逐行模式，不烧模型
node $H\src\board.mjs literal    story2video\examples\my.json

# 只有一个点子：先出故事，再自由编译
node $H\src\board.mjs story      story2video\stories\idea.txt --out $B
node $H\src\board.mjs from-story $B --shots 10

# 通用
node src/render.mjs <board.json> --workspace <会话工作区> --stage assets    [--plan|--adopt|--only id,id|--force]
node src/render.mjs <board.json> --workspace <会话工作区> --stage keyframes [--shots s01,s03|--force|--no-assets]
node src/render.mjs <board.json> --workspace <会话工作区> --stage clips|assemble|all
```

**必须显式给 `--workspace`**（或设 `AIH_WORKSPACE`）：数据跟着项目走、代码位置固定，
从工程目录跑会把资产生成到工程里去（实测踩过）。工作区里没有 `story2video/` 会直接拒绝运行。

`--plan` 只看计划不出图；`--adopt` 把磁盘上已有的产物认领回板子（崩过之后不用重新烧钱）。

```powershell
node $H\src\board.mjs table      $B
node $H\src\board.mjs approve    $B --stage story --by 用户
node $H\src\board.mjs validate   $B
node $H\src\migrate.mjs <旧.json> --out <新.json>
```

---

## 可编程验收（不许"我觉得还行"）

```powershell
node tests/contract.test.mjs <board.json>   # 9 项：真实板子必须通过，每一类错误必须被拒
node tests/compiler.test.mjs                # 16 项：补强层能整理脏输出，但不抹平真错误
node tests/scenes.test.mjs                  # 24 项：场次确定性解析，认不出时不瞎猜
node tests/literal.test.mjs <board.json>    # 14 项：台词与原剧本逐字 diff = 0
node tests/gate.test.mjs                    # 5 项：闸门只看用户的票，不看自己推进到哪
node tests/assets.test.mjs <board.json>     # 44 项：资产配方可断言，出图前就知道对不对
node tests/score.test.mjs                   # 36 项：判据可断言，总分不能替脸和服装说话
node tests/orchestrate.test.mjs <board.json> # 56 项：帧落网格、总长=各镜之和、字幕首尾相接
node tests/review.test.mjs                  # 13 项：四种病（全黑/静音/时长不符/流损坏）都得拦下
node tests/cost.test.mjs                    # 23 项：按张、按字符、不认识的模型会喊出来
```

| 阶段 | 验收 | 状态 |
|---|---|---|
| ① 契约 | contract / compiler / scenes / gate | ✅ 完成 |
| ② 逐行 | `literal.test.mjs` —— **台词逐字 diff = 0** | ✅ 完成 |
| ③ 资产 | `assets.test.mjs` + 线上通道实测出图 + **两张身份图都过打分线** | ✅ 完成 |
| ④ 打分 | `score.test.mjs` + 资产池 + 关键帧池（s08 崩帧重抽） | ✅ 完成 |
| ④ 编排 | `orchestrate.test.mjs` + **成片 88.687s 已产出** | ✅ 完成 |

### 端到端成片（《大师兄的离谱负责》）

```
literal   剧本 32 行 → 14 镜 / 台词 12 句逐字 diff = 0
assets    7/7（2 肖像 + 2 身份图 + 3 场景图）　线上 46s/张
keyframes 14/14　本地 10-22s/张（--fast + 参考图降采样，7.5 倍）
tts       12/12　时长由配音**实测反推**：72s → 86.67s
clips     14/14　i2v 12 个 + fl2v 2 个（首尾帧夹逼）
assemble  88.687s / 2127 帧 / h264 + aac / mean −19.7 dB
          字幕 12 条（SRT），画面无烧字（14 条裁切带）
```

### 已实测的端到端结果（《大师兄的离谱负责》，12 句台词）

```
literal（逐行模式，不烧模型）
  剧本 32 行 → 14 镜 / 72s / 2 角色 / 2 造型 / 1 场景
  台词 12 句 → 产出 12 句，逐字 diff = 0，顺序不变
  每句台词都能溯源到剧本行号（shots[].source_lines）
  耗时 < 1 秒，0 轮重试

from-story（自由编译，作对照）
  第 1 轮 583.9s：2 error → 模型自己修 → 第 2 轮 134.9s：0 error
  2 角色 / 2 造型 / 1 场景 / 1 道具 / 10 镜 / 80s
  台词 12/12 命中原文（**靠提示词求它别改**）
```

### 线上通道实测（`bl image generate` / qwen-image-3.0）

```
肖像生成：46.3s，2048×2048，一次成功
配方执行：正面朝向 ✓　脸占 60-70% ✓　纯灰底 ✓　素色上衣 ✓
         无服装细节 ✓　无场景 ✓　无可读文字 ✓

族裔锚点对照（同一份配方，唯一差别是加没加 regionAnchor）：
  没有锚点 → 白人青年
  加了锚点 → 东亚青年 ✅
```

---

## 从 DramaClaw 提炼的判断

见 `docs/drama-claw-recon.md`。已经落进本契约的：

- **身份/造型分离** ← `CharacterIdentity`「核心设计：身份才是本质」
- **脸/服装分字段** ← `face_prompt` vs `appearance_details`
- **双层参考** ← Portrait（脸占 60-70%、纯灰底、禁服装）→ 4 面板 sheet
- **标记协议** ← `{{identity_id}}` / `[[prop_id]]` + 菜单校验 + 禁裸 id
- **禁止或者/二选一** ← `LiteralBeatMetaOutput` 的校验器
- **场次正则优先** ← `screenplay_scene_parser.py`（正在补）
- **beats 带 purpose** ← 「为什么这一拍必须在片子里」

**还没拿的**：候选池 + 逐维打分（face_score ≥ 7 AND clothing_score ≥ 7）、
失败模式库 → 负向提示词注入、FLF 方向反转、时长由音频反推。这些排在后两个阶段。
